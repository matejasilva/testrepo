# Formatting Utilities

Number/string formatting: pluralize, format_number, truncate_str, pad_left/right, percent_str.

```python
from formatting_utils import pluralize, format_number, truncate_str, percent_str
```

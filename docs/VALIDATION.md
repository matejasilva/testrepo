# Validation Utilities

Input validation helpers: email, URL, positive int, non-empty string, range, one-of, required keys.

```python
from validation import is_email, is_url, is_positive_int, in_range, has_required_keys
```

# Regex Utilities

Common regex patterns and helpers: extract first/all, replace all, split. Includes EMAIL_PATTERN, URL_PATTERN, etc.

```python
from regex_utils import extract_first, extract_all, replace_all, EMAIL_PATTERN
```
